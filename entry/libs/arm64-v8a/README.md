# Native Libraries

Place the real HarmonyOS arm64 mihomo adapter here as:

```text
libmihomo_adapter.so
```

The placeholder README keeps the directory visible in the project. Do not rename the adapter library unless `entry/src/main/cpp/native/mihomo_adapter.cpp` is updated too.
